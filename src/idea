## 1.全局异常和全局返回值 设计思路
 自定义业务异常继承运行时异常，然后封装一个统一的返回值，所以的控制器都按照返回这个返回值，
 自定义handler，使用@ontrollerAdvice进行拦截，如果是业务异常，直接返回，如果是非业务异常需要进行封装进行返回。
 这中做法有个缺点就是所有的控制器返回值都得一样，后续考虑一下如何在控制器不需要使用统一的返回值。swagger应该有
 此功能。
 
 
 ## 2.使用mybatis-plus中遇到的问题
 
 yml添加 mybaitsPlus 配置文件，可以查询到数据，不添加就查询不到，还没找到具体原因
 
